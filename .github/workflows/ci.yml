name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm test
      - run: npm install -g @elgato/cli
      - name: Validate plugins
        run: |
          for dir in plugins/*/*.sdPlugin; do
            echo "validating $dir"
            streamdeck validate "$dir"
          done

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: npm install -g @elgato/cli
      - name: Pack plugins
        run: |
          for dir in plugins/*/*.sdPlugin; do
            streamdeck pack "$dir" --output dist-packs
          done
      - uses: softprops/action-gh-release@v2
        with:
          files: dist-packs/*.streamDeckPlugin
